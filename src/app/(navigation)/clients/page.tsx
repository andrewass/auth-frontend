import ClientsList from "@/app/(navigation)/clients/ClientsList";

const Clients = () => {

    return (
        <div>
            Clients
            <ClientsList/>
        </div>
    );
}

export default Clients;